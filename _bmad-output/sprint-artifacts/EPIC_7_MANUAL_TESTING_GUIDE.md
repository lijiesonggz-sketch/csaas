# EPIC 7 运营管理与成本优化 - 手工验证手册

## 文档信息

**Epic**: EPIC 7 - 运营管理与成本优化
**版本**: 1.0
**创建日期**: 2026-02-05
**适用环境**: 测试环境、UAT 环境
**测试角色**: 平台管理员、普通用户

---

## 测试环境准备

### 前置条件

1. **测试账号**
   - 管理员账号: `admin@test.com` / `Test123456`
   - 普通用户账号: `user1@org1.com` / `Test123456`
   - 组织数量: 至少 10 个测试组织

2. **测试数据**
   - 推送记录: 至少 100 条（包含成功、失败状态）
   - AI 调用记录: 至少 50 条（覆盖所有任务类型）
   - 用户反馈: 至少 30 条（评分 1-5 星）
   - 客户活动日志: 至少 30 天的活动记录

3. **环境配置**
   - 后端服务运行在 `http://localhost:3000`
   - 前端应用运行在 `http://localhost:3001`
   - Redis 缓存服务正常
   - PostgreSQL 数据库已执行所有 migrations

4. **数据准备脚本**
   ```bash
   # 运行测试数据初始化脚本
   cd backend
   npm run seed:epic7-test-data
   ```

---

## Story 7.1: 运营仪表板 - 系统健康监控

### [P0] AC1: 运营仪表板页面基础结构

**测试目标**: 验证运营仪表板页面基本布局和元素显示

**验证步骤**:
1. 使用管理员账号登录系统
2. 在左侧导航菜单中点击 **"运营仪表板"**
3. 页面加载完成后，检查页面元素

**预期结果**:
- [ ] 页面标题显示 "运营仪表板"
- [ ] 显示 4 个核心指标卡片:
  - 可用性指标卡片
  - 推送成功率指标卡片
  - AI 成本指标卡片
  - 客户活跃度指标卡片
- [ ] 显示异常告警区域（标题: "异常告警"）
- [ ] 显示健康趋势图表区域（标题: "健康趋势"）
- [ ] 页面布局响应式，适配桌面和平板

**验证数据**: 无需特殊数据

**优先级**: P0

---

### [P0] AC2: 系统可用性监控

**测试目标**: 验证系统可用性指标的计算和显示

**验证步骤**:
1. 访问运营仪表板页面
2. 查看 "可用性" 指标卡片
3. 检查数值和状态

**预期结果**:
- [ ] 显示当前可用性百分比（格式: XX.X%）
- [ ] 显示目标值（MVP 阶段: ≥99.5%）
- [ ] 如果可用性 >= 99.5%，卡片显示绿色或正常状态
- [ ] 如果可用性 < 99.5%，卡片显示红色告警状态
- [ ] 鼠标悬停显示详细信息:
  - 总运行时间（小时）
  - 停机时间（分钟）
  - 计算公式说明

**验证数据**:
- 正常状态: 确保 `system_health_logs` 表中最近 24 小时内 uptime 记录占比 >= 99.5%
- 异常状态: 手动插入 downtime 记录，使可用性 < 99.5%

**优先级**: P0

---

### [P0] AC3: 推送成功率监控

**测试目标**: 验证推送成功率指标的准确性

**验证步骤**:
1. 访问运营仪表板页面
2. 查看 "推送成功率" 指标卡片
3. 记录显示的数值

**预期结果**:
- [ ] 显示当前推送成功率（格式: XX.X%）
- [ ] 显示目标值（≥98%）
- [ ] 如果成功率 >= 98%，显示绿色正常状态
- [ ] 如果成功率 < 98%，显示红色告警状态
- [ ] 显示今日推送统计:
  - 总推送数
  - 成功数
  - 失败数
- [ ] 点击卡片，跳转到推送详情页面

**验证数据**:
```sql
-- 验证数据准确性
SELECT
  COUNT(*) as total,
  COUNT(CASE WHEN status = 'sent' THEN 1 END) as success,
  COUNT(CASE WHEN status = 'failed' THEN 1 END) as failed,
  (COUNT(CASE WHEN status = 'sent' THEN 1 END)::float / COUNT(*) * 100) as success_rate
FROM radar_pushes
WHERE DATE(created_at) = CURRENT_DATE;
```

**优先级**: P0

---

### [P1] AC4: AI 成本监控

**测试目标**: 验证 AI 成本指��的显示

**验证步骤**:
1. 访问运营仪表板页面
2. 查看 "AI 成本" 指标卡片
3. 检查数值和状态

**预期结果**:
- [ ] 显示今日 AI 成本（格式: ¥XX.XX）
- [ ] 显示本月累计成本（格式: ¥XX,XXX.XX）
- [ ] 显示单客户平均成本（格式: ¥XXX.XX）
- [ ] 显示目标值（单客户月均 < ¥500）
- [ ] 如果超标，显示红色告警状态
- [ ] 显示成本趋势（与上月对比）:
  - 上升趋势: 红色向上箭头 + 百分比
  - 下降趋势: 绿色向下箭头 + 百分比

**验证数据**:
```sql
-- 查询 AI 成本数据
SELECT
  SUM(CASE WHEN DATE(created_at) = CURRENT_DATE THEN cost ELSE 0 END) as today_cost,
  SUM(CASE WHEN DATE_TRUNC('month', created_at) = DATE_TRUNC('month', CURRENT_DATE) THEN cost ELSE 0 END) as month_cost,
  (SUM(CASE WHEN DATE_TRUNC('month', created_at) = DATE_TRUNC('month', CURRENT_DATE) THEN cost ELSE 0 END) / COUNT(DISTINCT organization_id)) as avg_per_client
FROM ai_usage_logs;
```

**优先级**: P1

---

### [P1] AC5: 客户活跃度监控

**测试目标**: 验证客户活跃度指标的准确性

**验证步骤**:
1. 访问运营仪表板页面
2. 查看 "客户活跃度" 指标卡片
3. 检查数值和分布

**预期结果**:
- [ ] 显示总客户数
- [ ] 显示活跃客户数（最近 7 天内有活动）
- [ ] 显示活跃率百分比（格式: XX.X%）
- [ ] 显示目标值（活跃率 > 70%）
- [ ] 显示客户活跃度分布:
  - 高活跃（>85%）: X 个客户
  - 中活跃（60-85%）: X 个客户
  - 低活跃（<60%）: X 个客户
- [ ] 点击卡片，跳转到客户管理页面

**验证数据**:
```sql
-- 验证活跃客户统计
SELECT
  COUNT(*) as total,
  COUNT(CASE WHEN last_active_at >= NOW() - INTERVAL '7 days' THEN 1 END) as active,
  (COUNT(CASE WHEN last_active_at >= NOW() - INTERVAL '7 days' THEN 1 END)::float / COUNT(*) * 100) as active_rate
FROM organizations;
```

**优先级**: P1

---

### [P0] AC6: 异常告警显示

**测试目标**: 验证异常告警列表的显示和功能

**验证步骤**:
1. 访问运营仪表板页面
2. 滚动到 "异常告警" 区域
3. 查看告警列表

**预期结果**:
- [ ] 显示告警列表（最新告警在上）
- [ ] 每条告警包含:
  - 告警类型标签（爬虫失败/AI成本超标/客户流失风险/推送失败率高）
  - 严重程度标识（高/中/低，不同颜色）
  - 发生时间（相对时间，如 "2 小时前"）
  - 描述信息
  - 快速操作按钮（"查看详情" / "标记已处理"）
- [ ] 未处理告警数量显示在页面顶部（红色徽章）
- [ ] 支持筛选（按严重程度、按告警类型）
- [ ] 点击 "标记已处理" 后，告警状态更新为 "已处理"

**验证数据**:
- 准备不同类型和严重程度的告警记录
- 至少包含 5 条未处理告警

**优先级**: P0

---

### [P1] AC7: 健康趋势图表

**测试目标**: 验证健康趋势图表的渲染和交互

**验证步骤**:
1. 访问运营仪表板页面
2. 滚动到 "健康趋势" 区域
3. 查看趋势图表

**预期结果**:
- [ ] 显示最近 30 天的趋势图表（默认）:
  - 可用性趋势（折线图，蓝色线条）
  - 推送成功率趋势（折线图，绿色线条）
  - AI 成本趋势（折线图，橙色线条）
- [ ] X 轴显示日期（格式: MM-DD）
- [ ] Y 轴显示数值（左侧: 百分比，右侧: 成本金额）
- [ ] 支持时间范围切换:
  - 点击 "7天" 按钮，图表更新为最近 7 天数据
  - 点击 "30天" 按钮，图表更新为最近 30 天数据
  - 点击 "90天" 按钮，图表更新为最近 90 天数据
- [ ] 鼠标悬停显示具体数值（Tooltip）
- [ ] 支持导出功能:
  - 点击 "导出 CSV" 按钮，下载 CSV 文件
  - 文件包含日期、可用性、推送成功率、AI 成本列

**验证数据**:
- 确保 `system_health_logs` 表中有至少 90 天的历史数据

**优先级**: P1

---

### [P1] AC8: 实时数据更新

**测试目标**: 验证仪表板数据的自动刷新功能

**验证步骤**:
1. 访问运营仪表板页面
2. 记录当前显示的指标数值
3. 等待 30 秒
4. 观察页面变化

**预期结果**:
- [ ] 页面底部显示 "最后更新时间"（格式: HH:MM:SS）
- [ ] 每 30 秒自动刷新指标数据
- [ ] 刷新时，最后更新时间更新为当前时间
- [ ] 如果数据发生变化，指标卡片数值更新
- [ ] 点击 "手动刷新" 按钮，立即刷新数据
- [ ] 刷新过程中，显示加载动画（可选）

**验证数据**:
- 在另一个浏览器窗口中手动修改数据（如插入新告警）
- 等待自动刷新，验证数据是否更新

**优先级**: P1

---

## Story 7.2: 内容质量管理

### [P1] AC1: 推送详情页显示内容评分区域

**测试目标**: 验证推送详情弹窗中的评分区域显示

**验证步骤**:
1. 使用普通用户账号登录系统
2. 访问 **"推送历史"** 页面
3. 点击任意一条推送记录的 "查看详情" 按钮
4. 滚动到弹窗底部

**预期结果**:
- [ ] 弹窗底部显示 "内容评分" 区域（有分隔线）
- [ ] 显示 5 星评分组件（可点击星星）
- [ ] 显示可选文字反馈输入框（Placeholder: "可选：请输入您的反馈..."）
- [ ] 显示提示文字: "您的反馈帮助我们改进服务"
- [ ] 显示 "提交反馈" 按钮（默认禁用，选择评分后启用）

**验证数据**: 至少 1 条推送记录

**优先级**: P1

---

### [P1] AC2: 用户提交评分创建反馈记录

**测试目标**: 验证用户提交评分的完整流程

**验证步骤**:
1. 在推送详情弹窗中，点击 **4 星** 评分
2. 在评论框中输入: "内容很有用，帮助我了解了最新的技术趋势"
3. 点击 "提交反馈" 按钮
4. 等待提交完成

**预期结果**:
- [ ] 提交按钮显示 "提交中..." 加载状态
- [ ] 提交成功后，显示绿色提示框: "感谢您的反馈！"
- [ ] 评分区域更新为 "已评分" 状态，显示用户提交的评分
- [ ] 表单禁用，不允许重复提交
- [ ] 数据库中创建 `push_feedback` 记录:
  ```sql
  SELECT * FROM push_feedback
  WHERE push_id = '<推送ID>' AND user_id = '<用户ID>';
  -- 验证: rating = 4, comment = '内容很有用...'
  ```

**验证数据**: 确保用户未对该推送提交过反馈

**优先级**: P1

---

### [P2] AC2-重复提交验证: 用户不能重复评分同一推送

**测试目标**: 验证重复评分的防护机制

**验证步骤**:
1. 对同一条推送再次打开详情弹窗
2. 检查评分区域状态

**预期结果**:
- [ ] 评分区域显示 "您已评分" 状态
- [ ] 显示之前提交的评分（如 4 星）
- [ ] 评分和评论表单禁用，无法修改
- [ ] 不显示 "提交反馈" 按钮

**验证数据**: 该推送已有用户反馈记录

**优先级**: P2

---

### [P1] AC3: 内容质量管理页面基础结构

**测试目标**: 验证内容质量管理页面布局

**验证步骤**:
1. 使用管理员账号登录系统
2. 在左侧导航菜单中点击 **"内容质量管理"**
3. 页面加载完成后，检查页面元素

**预期结果**:
- [ ] 页面标题显示 "内容质量管理"
- [ ] 显示 4 个指标卡片:
  - 平均评分（格式: X.X / 5.0）
  - 总反馈数
  - 低分推送数（评分 < 3.0）
  - 目标达成率（评分 >= 4.0 的推送占比）
- [ ] 显示评分分布图表（5 星分布条形图）
- [ ] 显示低分推送列表（标题: "低分推送列表"）
- [ ] 显示内容质量趋势图表（标题: "内容质量趋势"）

**验证数据**: 至少 30 条推送反馈记录

**优先级**: P1

---

### [P1] AC4: 低分推送识别与列表

**测试目标**: 验证低分推送的识别和显示

**验证步骤**:
1. 访问内容质量管理页面
2. 滚动到 "低分推送列表" 区域
3. 查看推送列表

**预期结果**:
- [ ] 只显示平均评分 < 3.0 的推送
- [ ] 推送按评分升序排列（最低分在最上面）
- [ ] 每条推送显示:
  - 推送标题
  - 平均评分（红色高亮，格式: X.X / 5.0）
  - 反馈数量（格式: X 条反馈）
  - 雷达类型标签（技术/行业/合规）
  - "查看详情" 按钮
- [ ] 支持按雷达类型筛选:
  - 选择 "技术雷达"，只显示技术类推送
  - 选择 "行业雷达"，只显示行业类推送
  - 选择 "合规雷达"，只显示合规类推送

**验证数据**:
```sql
-- 准备低分推送数据
SELECT p.id, p.title, AVG(f.rating) as avg_rating, COUNT(f.id) as feedback_count
FROM radar_pushes p
LEFT JOIN push_feedback f ON p.id = f.push_id
GROUP BY p.id
HAVING AVG(f.rating) < 3.0
ORDER BY avg_rating ASC;
```

**优先级**: P1

---

### [P1] AC5: 低分推送详情查看与处理

**测试目标**: 验证低分推送的详情弹窗和处理功能

**验证步骤**:
1. 在低分推送列表中，点击第一条推送的 "查看详情" 按钮
2. 弹窗打开后，检查内容
3. 测试处理操作

**预期结果**:
- [ ] 弹窗显示推送完整内容:
  - 推送标题
  - 推送摘要
  - 推送全文
  - 雷达类型
  - 相关性评分
  - 信息源
- [ ] 显示所有用户反馈列表（按时间倒序）:
  - 每条反馈包含: 用户姓名、评分（星级）、评论��提交时间
- [ ] 显示 AI 分析结果:
  - 相关性评分（relevanceScore）
  - 内容质量评分（qualityScore，如有）
- [ ] 显示优化建议列表:
  - 如相关性评分高但用户评分低: "相关性评分过高（0.XX），但用户反馈显示内容不够相关，建议调整AI相关性算法权重"
  - 如内容质量问题: "内容质量不佳，建议优化信息源筛选"
  - 如推送频率过高: "建议降低推送数量上限"
- [ ] 显示两个操作按钮:
  - "标记为已优化" 按钮（蓝色）
  - "忽略" 按钮（灰色）
- [ ] 点击 "标记为已优化":
  - 显示确认提示
  - 确认后，弹窗关闭
  - 该推送状态更新为 "已优化"（显示绿色徽章）
  - 推送从列表中移除或标记为已处理

**验证数据**: 选择一条有多条负面反馈的低分推送

**优先级**: P1

---

### [P1] AC6: 内容质量趋势分析

**测试目标**: 验证内容质量趋势图表的准确性

**验证步骤**:
1. 访问内容质量管理页面
2. 滚动到 "内容质量趋势" 区域
3. 查看趋势图表

**预期结果**:
- [ ] 显示最近 30 天的趋势图表（默认）:
  - 平均评分趋势（折线图，蓝色线条）
  - 低分推送数量趋势（折线图或柱状图，红色）
- [ ] 支持按雷达类型分组显示:
  - 点击 "技术雷达" 标签，显示技术类推送的趋势
  - 点击 "行业雷达" 标签，显示行业类推送的趋势
  - 点击 "合规雷达" 标签，显示合规类推送的趋势
  - 点击 "全部" 标签，显示所有推送的趋势
- [ ] 图表中标注目标线（平均评分 = 4.0，红色虚线）
- [ ] 鼠标悬停显示具体数值

**验证数据**: 确保有至少 30 天的反馈数据

**优先级**: P1

---

## Story 7.3: 客户管理与流失风险预警

### [P1] AC1: 客户管理页面复用与扩展

**测试目标**: 验证客户管理页面增强功能

**验证步骤**:
1. 使用管理员账号登录系统
2. 访问 **"客户管理"** 页面（/admin/clients）
3. 查看客户列表

**预期结果**:
- [ ] 复用 Story 6.2 的客户列表布局
- [ ] 每个客户卡片额外显示 "月活率" 指标:
  - 格式: XX.X%
  - 颜色编码:
    - 绿色: 月活率 >= 85%（高活跃）
    - 黄色: 月活率 60-85%（中活跃）
    - 红色: 月活率 < 60%（低活跃/流失风险）
- [ ] 支持按月活率排序:
  - 点击 "月活率" 列头，升序排列
  - 再次点击，降序排列
- [ ] 支持按月活率筛选:
  - 筛选器选项: 全部/高活跃/中活跃/低活跃/流失风险
  - 选择 "流失风险"，只显示月活率 < 60% 的客户

**验证数据**: 至少 10 个客户，覆盖不同活跃度

**优先级**: P1

---

### [P1] AC2: 月活率计算

**测试目标**: 验证月活率计算的准确性

**验证步骤**:
1. 选择一个客户（如 "杭州银行"）
2. 记录其月活率显示值
3. 查询数据库验证

**预期结果**:
- [ ] 月活率计算公式正确:
  - 月活率 = (最近 30 天活跃天数 / 30) × 100%
  - 活跃定义: 登录系统 OR 查看推送 OR 提交反馈
- [ ] 页面显示值与数据库计算一致:
  ```sql
  -- 验证月活率计算
  SELECT
    organization_id,
    COUNT(DISTINCT activity_date) as active_days,
    (COUNT(DISTINCT activity_date)::float / 30 * 100) as activity_rate
  FROM customer_activity_logs
  WHERE organization_id = '<客户ID>'
    AND activity_date >= CURRENT_DATE - INTERVAL '30 days'
  GROUP BY organization_id;
  ```
- [ ] 显示活跃度分解:
  - 登录活跃度（如 90%）
  - 内容消费活跃度（如 85%）
  - 行动活跃度（如 60%）

**验证数据**: 选择一个有完整 30 天活动记录的客户

**优先级**: P1

---

### [P0] AC3: 流失风险预警

**测试目标**: 验证流失风险预警的触发机制

**验证步骤**:
1. 准备一个月活率 < 60% 的客户
2. 运行成本告警 cron job 或等待定时任务执行
3. 检查告警通知

**预期结果**:
- [ ] 客户卡片显示红色 "流失风险" 告警标识（闪烁或醒目标记）
- [ ] 创建告警记录:
  ```sql
  SELECT * FROM alerts
  WHERE alert_type = 'customer_churn_risk'
    AND metadata->>'organizationId' = '<客户ID>';
  -- 验证: severity = 'high', status = 'unresolved'
  ```
- [ ] 发送告警邮件到管理员邮箱:
  - 主题: "客户流失风险预警 - [客户名称]"
  - 内容包含: 客户名称、月活率、建议操作
- [ ] 运营仪表板显示该告警（异常告警列表）
- [ ] 提示信息: "客户 [名称] 月活率 < 60%，建议联系客户"

**验证数据**:
- 手动修改客户活动日志，使月活率 < 60%
- 或使用测试脚本创建低活跃客户

**优先级**: P0

---

### [P1] AC4: 流失风险客户筛选

**测试目标**: 验证流失风险客户筛选功能

**验证步骤**:
1. 在客户管理页面，点击筛选器
2. 选择 "流失风险" 选项
3. 查看筛选结果

**预期结果**:
- [ ] 只显示月活率 < 60% 的客户
- [ ] 客户按月活率升序排列（最低的在最前面）
- [ ] 每个客户卡片显示流失原因分析:
  - 如用户反馈平均评分 < 3.0: "推送内容不相关"
  - 如未读推送 > 20 条: "推送频率过高"
  - 如最近 30 天无设置更新: "功能不满足需求"
- [ ] 显示流失风险客户总数（如 "共 5 个客户处于流失风险"）

**验证数据**:
- 准备至少 3 个流失风险客户
- 确保每个客户有不同的流失原因

**优先级**: P1

---

### [P1] AC5: 管理员干预流失客户

**测试目标**: 验证客户干预工作流

**验证步骤**:
1. 在流失风险客户列表中，选择一个客户
2. 点击 "联系客户" 按钮
3. 弹窗打开，查看干预建议
4. 记录干预结果

**预期结果**:
- [ ] 弹窗显示客户联系信息:
  - 联系邮箱
  - 联系电话（如有）
  - 联系人姓名
- [ ] 显示干预建议列表:
  - 调整推送偏好（减少推送频率）
  - 增加关注领域（添加更多感兴趣的技术/行业）
  - 提供培训（如何更好使用系统）
- [ ] 干预结果记录表单:
  - 干预类型（单选）: 联系/调研/培训/配置调整
  - 干预结果（单选）: 已联系/已解决/已流失/待处理
  - 备注（文本框，最多 500 字）
- [ ] 点击 "保存干预记录" 按钮:
  - 创建 `customer_interventions` 记录
  - 弹窗关闭
  - 客户卡片显示 "已干预" 标识
  - 干预历史记录更新

**验证数据**: 选择一个流失风险客户

**优先级**: P1

---

### [P1] AC6: 客户细分分析

**测试目标**: 验证客户细分图表的准确性

**验证步骤**:
1. 访问运营仪表板或客户管理页面
2. 查看 "客户细分" 区域
3. 检查细分图表

**预期结果**:
- [ ] 显示客户细分饼图或柱状图:
  - 高活跃（>85%）: X 个客户，占比 XX%（绿色）
  - 中活跃（60-85%）: X 个客户，占比 XX%（黄色）
  - 低活跃（<60%）: X 个客户，占比 XX%（红色）
- [ ] 显示总客户数
- [ ] 显示目标线: 高活跃客户占比 > 70%（红色虚线）
- [ ] 如果高活跃客户占比 >= 70%，显示 "达标" 状态（绿色勾选）
- [ ] 如果高活跃客户占比 < 70%，显示 "未达标" 状态（红色感叹号）
- [ ] 点击细分区域，可查看该细分的客户列表

**验证数据**:
```sql
-- 验证客户细分统计
SELECT
  CASE
    WHEN monthly_activity_rate > 85 THEN 'high_active'
    WHEN monthly_activity_rate >= 60 THEN 'medium_active'
    ELSE 'low_active'
  END as segment,
  COUNT(*) as count,
  (COUNT(*)::float / (SELECT COUNT(*) FROM organizations) * 100) as percentage
FROM organizations
GROUP BY segment;
```

**优先级**: P1

---

## Story 7.4: AI 成本优化工具

### [P1] AC1: AI 成本追踪数据模型

**测试目标**: 验证 AI 调用日志的自动记录

**验证步骤**:
1. 使用普��用户账号登录系统
2. 上传一个文档到项目，触发 AI 分析
3. 等待分析完成
4. 查询数据库验证日志记录

**预期结果**:
- [ ] `ai_usage_logs` 表中创建新记录:
  ```sql
  SELECT * FROM ai_usage_logs
  WHERE organization_id = '<用户组织ID>'
  ORDER BY created_at DESC
  LIMIT 1;
  ```
- [ ] 记录包含完整字段:
  - id（UUID）
  - organization_id（用户组织 ID）
  - task_type（如 'tech_analysis'）
  - model_name（如 'qwen-plus'）
  - input_tokens（输入 token 数，> 0）
  - output_tokens（输出 token 数，> 0）
  - cost（成本，精确到分）
  - request_id（请求 ID，可为 null）
  - created_at（创建时间）
- [ ] 成本计算正确:
  - cost = (input_tokens × 0.008 / 1000) + (output_tokens × 0.02 / 1000)
  - 精确到 2 位小数

**验证数据**: 任意文档，能触发 AI 分析即可

**优先级**: P1

---

### [P2] AC2: AI 调用拦截器实现

**测试目标**: 验证拦截器覆盖所有 AI 调用场景

**验证步骤**:
1. 分别触发不同类型的 AI 分析:
   - 技术雷达 AI 分析（上传技术文档）
   - 行业雷达 AI 分析（上传行业案例）
   - 合规雷达 AI 分析（上传合规文件）
   - ROI 计算（查看技术 ROI）
   - 剧本生成（查看合规应对剧本）
2. 查询数据库，验证每次调用都有日志

**预期结果**:
- [ ] 每种 AI 调用类型都有对应的日志记录
- [ ] task_type 字段正确标记:
  - 技术分析: 'tech_analysis'
  - 行业分析: 'industry_analysis'
  - 合规分析: 'compliance_analysis'
  - ROI 计算: 'roi_calculation'
  - 剧本生成: 'playbook_generation'
- [ ] 拦截器不阻塞业务逻辑（即使日志失败，AI 分析仍正常返回结果）

**验证数据**: 覆盖所有 AI 使用场景的测试文档

**优先级**: P2

---

### [P1] AC3: 成本优化页面 - 总览指标

**测试目标**: 验证成本优化页面的基础布局和指标

**验证步骤**:
1. 使用管理员账号登录系统
2. 在左侧导航菜单中点击 **"成本优化"**
3. 页面加载完成后，检查页面元素

**预期结果**:
- [ ] 页面标题显示 "AI 成本优化"
- [ ] 显示 4 个指标卡片:
  - 今日总成本（格式: ¥XX.XX）
  - 本月累计成本（格式: ¥X,XXX.XX）
  - 单客户平均成本（格式: ¥XXX.XX）
  - 成本超标客户数量（整数）
- [ ] 如果单客户平均成本 > ¥500，卡片显示红色告警状态
- [ ] 显示 "批量优化" 按钮（默认禁用）
- [ ] 显示高成本客户列表（标题: "高成本客户"）
- [ ] 显示成本趋势图表（标题: "成本趋势"）

**验证数据**: 至少 50 条 AI 调用记录

**优先级**: P1

---

### [P0] AC4: 单客户成本计算与告警

**测试目标**: 验证单客户成本超标告警机制

**验证步骤**:
1. 准备一个月度成本 > ¥500 的客户（可手动插入测试数据）
2. 运行成本告警 cron job 或等待定时任务执行
3. 检查告警通知

**预期结果**:
- [ ] 客户卡片显示 "成本超标" 红色标识
- [ ] 创建告警记录:
  ```sql
  SELECT * FROM alerts
  WHERE alert_type = 'ai_cost_exceeded'
    AND metadata->>'organizationId' = '<客户ID>';
  -- 验证: severity = 'high', status = 'unresolved'
  ```
- [ ] 发送告警邮件到管理员邮箱:
  - 主题: "AI 成本超标预警 - [客户名称]"
  - 内容包含: 客户名称、月度成本、超标金额、优化建议
- [ ] 运营仪表板显示该告警
- [ ] 成本优化页面的 "成本超标客户数量" 指标 +1

**验证数据**:
```sql
-- 手动插入高成本测试数据
INSERT INTO ai_usage_logs (organization_id, task_type, input_tokens, output_tokens, cost, created_at)
SELECT
  '<客户ID>',
  'tech_analysis',
  10000,
  5000,
  (10000 * 0.008 / 1000 + 5000 * 0.02 / 1000),
  NOW() - (random() * INTERVAL '30 days')
FROM generate_series(1, 100);
-- 生成 100 条记录，总成本约 ¥580
```

**优先级**: P0

---

### [P1] AC5: 成本分解与优化建议

**测试目标**: 验证成本分解图表和优化建议的准确性

**验证步骤**:
1. 在高成本客户列表中，点击一个成本超标客户的 "查看详情" 按钮
2. 弹窗打开，查看成本分解
3. 检查优化建议

**预期结果**:
- [ ] 显示成本分解饼图:
  - 技术雷达分析任务成本占比（如 50%）
  - 行业雷达分析任务成本占比（如 20%）
  - 合规雷达分析任务成本占比（如 15%）
  - ROI 计算任务成本占比（如 10%）
  - 剧本生成任务成本占比（如 5%）
- [ ] 显示每种任务的详细统计:
  - 调用次数
  - 总 token 数
  - 平均每次调用成本
  - 成本小计
- [ ] 显示优化建议列表（基于规则引擎）:
  - 如果技术雷达占比 > 50%: "建议减少技术雷达推送频率或启用缓存"
  - 如果剧本生成占比 > 30%: "建议优化剧本 prompt 长度"
  - 如果总推送数量 > 100/月: "建议降低推送数量上限"
  - 如果低分推送占比 > 20%: "建议启用更严格的相关性过滤"
- [ ] 每条建议包含:
  - 建议描述
  - 影响程度（高/中/低）
  - 可操作步骤（如 "调整推送偏好设置"）

**验证数据**: 选择一个有完整成本分解的客户

**优先级**: P1

---

### [P1] AC6: 成本趋势分析

**测试目标**: 验证成本趋势图表的功能

**验证步骤**:
1. 访问成本优化页面
2. 查看 "成本趋势" 图表
3. 测试交互功能

**预期结果**:
- [ ] 显示最近 90 天的总成本趋势图（默认，折线图）
- [ ] 显示单客户平均成本趋势图（折线图）
- [ ] 显示 Top 10 高成本客户排名（条形图或表格）
- [ ] 支持时间范围切换:
  - 点击 "7天"，显示最近 7 天数据
  - 点击 "30天"，显示最近 30 天数据
  - 点击 "90天"，显示最近 90 天数据
- [ ] 支持导出功能:
  - 点击 "导出报告" 按钮
  - 选择格式（CSV 或 Excel）
  - 下载文件，文件包含:
    - 日期
    - 总成本
    - 单客户平均成本
    - Top 10 客户名称和成本

**验证数据**: 确保有至少 90 天的 AI 调用记录

**优先级**: P1

---

### [P1] AC7: 批量成本优化

**测试目标**: 验证批量成本优化功能的完整流程

**验证步骤**:
1. 在成本优化页面，勾选 3 个高成本客户（使用复选框）
2. 点击页面顶部的 "批量优化" 按钮
3. 在弹窗中选择优化策略
4. 确认执行

**预期结果**:
- [ ] "批量优化" 按钮在选择客户后启用，显示选中数量（如 "批量优化 (3)"）
- [ ] 弹窗显示优化策略选择:
  - 选项 1: 降低推送上限（减少 30%，最小保留 10）
    - 说明: 从当前值减少 30%，如当前为 100，优化后为 70
  - 选项 2: 启用更严格的相关性过滤（minRelevanceScore 提升到 4.0）
    - 说明: 只推送相关性评分 >= 4.0 的内容
  - 选项 3: 禁用低 ROI 技术推送（minRoiScore 提升到 3.0）
    - 说明: 只推送 ROI 评分 >= 3.0 的技术
- [ ] 弹窗显示预计影响:
  - 预计减少推送数量: XX 条/月
  - 预计降低成本: ¥XX.XX/月
- [ ] 选择策略后，点击 "确认执行":
  - 显示加载动画
  - 执行批量更新 PushPreference 配置
  - 记录操作到 AuditLog 表:
    ```sql
    SELECT * FROM audit_logs
    WHERE action = 'batch_cost_optimize'
    ORDER BY created_at DESC
    LIMIT 1;
    -- 验证: target_ids 包含 3 个客户 ID
    ```
  - 显示成功提示: "成本优化策略已应用到 3 个客户"
  - 弹窗关闭
  - 客户列表刷新，客户卡片显示 "已优化" 标识
  - 取消所有复选框选中状态

**验证数据**:
- 准备至少 3 个高成本客户
- 确保客户有 PushPreference 配置记录

**优先级**: P1

---

## 跨Story集成测试

### [P0] 端到端场景 1: 新用户首次使用系统的完整体验

**测试目标**: 验证新用户从注册到使用的完整流程

**验证步骤**:
1. 创建新组织和用户账号
2. 用户登录，上传第一个文档
3. AI 分析完成，查看推送
4. 用户对推送进行评分
5. 管理员查看运营数据

**预期结果**:
- [ ] AI 分析自动记录成本日志（Story 7.4）
- [ ] 用户查看推送详情，提交反馈（Story 7.2）
- [ ] 用户活动自动记录到 customer_activity_logs（Story 7.3）
- [ ] 管理员在运营仪表板看到新客户（Story 7.1）
- [ ] 管理员在成本优化页面看到成本增加（Story 7.4）
- [ ] 管理员在内容质量页面看到新反馈（Story 7.2）

**优先级**: P0

---

### [P0] 端到端场景 2: 成本超标客户的完整干预流程

**测试目标**: 验证从成本超标检测到优化的完整流程

**验证步骤**:
1. 准备一个成本超标客户（月度成本 > ¥500）
2. 等待 cron job 触发告警（或手动触发）
3. 管理员收到告警邮件
4. 管理员访问成本优化页面
5. 管理员应用批量优化策略
6. 验证优化效果

**预期结果**:
- [ ] 成本超标告警自动创建（Story 7.4, AC4）
- [ ] 运营仪表板显示告警（Story 7.1, AC6）
- [ ] 管理员邮箱收到告警邮件（Story 7.4, AC4）
- [ ] 成本优化页面显示该客户（Story 7.4, AC3）
- [ ] 批量优化成功应用（Story 7.4, AC7）
- [ ] PushPreference 配置更新（验证数据库）
- [ ] AuditLog 记录操作（验证数据库）

**优先级**: P0

---

### [P1] 端到端场景 3: 低活跃客户的流失预警和干预

**测试目标**: 验证客户流失预警的完整流程

**验证步骤**:
1. 准备一个低活跃客户（月活率 < 60%）
2. 等待 cron job 触发告警
3. 管理员收到告警邮件
4. 管理员访问客户管理页面
5. 管理员联系客户并记录干预结果
6. 验证客户状态更新

**预期结果**:
- [ ] 流失风险告警自动创建（Story 7.3, AC3）
- [ ] 运营仪表板显示告警（Story 7.1, AC6）
- [ ] 管理员邮箱收到告警邮件（Story 7.3, AC3）
- [ ] 客户卡片显示流失风险标识（Story 7.3, AC3）
- [ ] 客户管理页面显示流失原因（Story 7.3, AC4）
- [ ] 干预记录成功保存（Story 7.3, AC5）
- [ ] 客户卡片显示 "已干预" 标识（Story 7.3, AC5）

**优先级**: P1

---

### [P1] 端到端场景 4: 低分推送的识别和优化

**测试目标**: 验证内容质量管理的完整流程

**验证步骤**:
1. 多个用户对同一推送提交低分反馈（< 3 分）
2. 推送平均评分降到 < 3.0
3. 管理员访问内容质量管理页面
4. 管理员查看低分推送详情
5. 管理员标记推送为 "已优化"

**预期结果**:
- [ ] 用户反馈成功提交（Story 7.2, AC2）
- [ ] 推送出现在低分推送列表（Story 7.2, AC4）
- [ ] 推送详情显示所有反馈（Story 7.2, AC5）
- [ ] 推送详情显示优化建议（Story 7.2, AC5）
- [ ] 推送状态更新为 "已优化"（Story 7.2, AC5）
- [ ] 推送从低分列表移除或标记（Story 7.2, AC5）

**优先级**: P1

---

## 性能测试场景

### [P2] 性能测试 1: 运营仪表板响应时间

**测试目标**: 验证运营仪表板在大数据量下的性能

**测试条件**:
- 系统健康日志: 100,000 条记录
- 推送记录: 50,000 条
- AI 调用记录: 100,000 条
- 客户数量: 1,000 个
- 并发用户: 10 个管理员

**验证步骤**:
1. 使用 JMeter 或 k6 创建性能测试脚本
2. 模拟 10 个并发用户同时访问运营仪表板
3. 记录响应时间

**预期结果**:
- [ ] 健康指标计算响应时间 < 2 秒（P95）
- [ ] 趋势数据查询响应时间 < 3 秒（P95）
- [ ] 页面首屏加载时间 < 1 秒（P95）
- [ ] Redis 缓存命中率 > 80%

**优先级**: P2

---

### [P2] 性能测试 2: 成本计算响应时间

**测试目标**: 验证成本计算在大数据量下的性能

**测试条件**:
- AI 调用记录: 1,000,000 条（100 天 × 1,000 客户 × 10 次/天）
- 客户数量: 1,000 个
- 并发用户: 5 个管理员

**验证步骤**:
1. 使用性能测试工具模拟 5 个并发用户访问成本优化页面
2. 每个用户分别查询单客户成本、成本趋势、成本分解
3. 记录响应时间

**预期结果**:
- [ ] 单客户成本查询 < 1 秒（P95）
- [ ] 成本趋势查询 < 3 秒（P95）
- [ ] 成本分解查询 < 2 秒（P95）
- [ ] 数据库索引有效使用（通过 EXPLAIN ANALYZE 验证）

**优先级**: P2

---

## 测试数据清理

### 测试完成后执行

**清理步骤**:
```sql
-- 清理测试数据（谨慎操作！）
DELETE FROM push_feedback WHERE created_at >= '<测试开始时间>';
DELETE FROM customer_activity_logs WHERE created_at >= '<测试开始时间>';
DELETE FROM customer_interventions WHERE created_at >= '<测试开始时间>';
DELETE FROM ai_usage_logs WHERE created_at >= '<测试开始时间>';
DELETE FROM alerts WHERE created_at >= '<测试开始时间>';
DELETE FROM system_health_logs WHERE created_at >= '<测试开始时间>';
DELETE FROM audit_logs WHERE created_at >= '<测试开始时间>';

-- 重置测试组织的配置
UPDATE organizations
SET monthly_activity_rate = NULL,
    activity_status = NULL,
    last_active_at = NULL
WHERE name LIKE '%测试%';
```

---

## 缺陷报告模板

### 缺陷报告格式

**缺陷 ID**: BUG-EPIC7-XXX
**Story**: Story 7.X
**AC**: ACXX
**严重程度**: Critical / High / Medium / Low
**优先级**: P0 / P1 / P2

**缺陷描述**:
简要描述缺陷现象

**复现步骤**:
1. 第一步
2. 第二步
3. 第三步

**预期结果**:
应该发生什么

**实际结果**:
实际发生了什么

**截图/日志**:
（附加截图或错误日志）

**测试环境**:
- 浏览器: Chrome 120
- 操作系统: Windows 11
- 后端版本: v1.0.0
- 前端版本: v1.0.0

---

## 验证通过标准

### Story 7.1: 运营仪表板
- [ ] 所有 P0 测试用例通过
- [ ] 所有 P1 测试用例通过率 >= 95%
- [ ] 无 Critical 或 High 严重程度缺陷
- [ ] 性能指标达标

### Story 7.2: 内容质量管理
- [ ] 所有 P1 测试用例通过
- [ ] 用户反馈功能无阻塞性缺陷
- [ ] 管理员功能完整可用

### Story 7.3: 客户管理与流失风险预警
- [ ] 所有 P0 测试用例通过
- [ ] 所有 P1 测试用例通过率 >= 95%
- [ ] 月活率计算准确率 100%
- [ ] 告警机制正常触发

### Story 7.4: AI 成本优化工具
- [ ] 所有 P0 测试用例通过
- [ ] 所有 P1 测试用例通过率 >= 95%
- [ ] AI 调用记录成功率 >= 99%
- [ ] 成本计算准确率 100%

### 整体验收标准
- [ ] 所有端到端场景测试通过
- [ ] 性能测试达标
- [ ] 无 P0 级别缺陷
- [ ] P1 级别缺陷 <= 3 个
- [ ] 用户体验流畅，无明显卡顿

---

## 附录

### 测试账号列表

| 角色 | 邮箱 | 密码 | 组织 |
|------|------|------|------|
| 平台管理员 | admin@test.com | Test123456 | - |
| 普通用户 1 | user1@org1.com | Test123456 | 杭州银行 |
| 普通用户 2 | user2@org2.com | Test123456 | 上海证券 |
| 普通用户 3 | user3@org3.com | Test123456 | 深圳科技 |

### 常用 SQL 查询

```sql
-- 查询最近创建的告警
SELECT * FROM alerts
ORDER BY created_at DESC
LIMIT 10;

-- 查询单客户月度成本
SELECT
  organization_id,
  SUM(cost) as total_cost,
  COUNT(*) as call_count
FROM ai_usage_logs
WHERE created_at >= DATE_TRUNC('month', CURRENT_DATE)
GROUP BY organization_id
ORDER BY total_cost DESC;

-- 查询低分推送
SELECT
  p.id,
  p.title,
  AVG(f.rating) as avg_rating,
  COUNT(f.id) as feedback_count
FROM radar_pushes p
LEFT JOIN push_feedback f ON p.id = f.push_id
GROUP BY p.id
HAVING AVG(f.rating) < 3.0
ORDER BY avg_rating ASC;

-- 查询客户活跃度
SELECT
  organization_id,
  COUNT(DISTINCT activity_date) as active_days,
  (COUNT(DISTINCT activity_date)::float / 30 * 100) as activity_rate
FROM customer_activity_logs
WHERE activity_date >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY organization_id
ORDER BY activity_rate ASC;
```

---

**测试完成日期**: ___________
**测试负责人**: ___________
**签字确认**: ___________
